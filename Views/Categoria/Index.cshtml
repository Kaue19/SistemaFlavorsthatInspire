@model IEnumerable<SistemaFlavorsThatInspire.Models.Categoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Categoria</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Criar Novo</a>
</p>
<div>
    @using (Html.BeginForm("Index", "Categoria", FormMethod.Get))
    {
        <p>
            Digite aqui sua pesquisa: @Html.TextBox("pesquisa")
            <input type="submit" value="Pesquisar" class="btn btn-primary" />
        </p>
    }
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoriaNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoriaNome)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.CategoriaId" class="btn btn-primary">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.CategoriaId" class="btn btn-primary">Detalhes</a> |
                    <a asp-action="Delete" asp-route-id="@item.CategoriaId" class="btn btn-segundo">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
